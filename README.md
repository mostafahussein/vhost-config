# Installation
Installation is simple:

    # cd /etc

    # git clone -n git://github.com/sanmai/vhost-config.git

    # mv vhost-config/.git .

    # chmod 700 .git

    # echo '*' > .git/info/exclude

    # git reset --hard

